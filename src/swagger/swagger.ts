import swaggerJSDoc from "swagger-jsdoc";

export const swaggerSpec = swaggerJSDoc({
  definition: {
    openapi: "3.0.0",
    info: {
      title: "Web Assignment 2 API",
      version: "1.0.0",
      description: "REST API for Users, Posts and Comments with JWT authentication",
    },
    servers: [
      {
        url: "http://localhost:3000",
        description: "Local server",
      },
    ],
    components: {
      securitySchemes: {
        bearerAuth: {
          type: "http",
          scheme: "bearer",
          bearerFormat: "JWT",
        },
      },
    },
    security: [
      {
        bearerAuth: [],
      },
    ],
  },

  // כאן Swagger סורק את ה־routes כדי למצוא הערות
  apis: ["./src/routes/*.ts"],
});
